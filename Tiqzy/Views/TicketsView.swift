import SwiftUI

struct TicketsView: View {
    var body: some View {
        Text("Tickets View")
            .font(.title)
            .foregroundColor(.green)
    }
}

#Preview {
    TicketsView()
}
